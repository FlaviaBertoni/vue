pipeline {
  agent any

  stages {
    stage('Dependences'){
      steps {
        echo "npm install"
      }
    }

    stage('Build'){
      steps {
        echo "npm run build"
      }
    }
  }
}
